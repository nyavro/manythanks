package com.nyavro.manythanks.ws.mark

import com.nyavro.manythanks.gcm.Transport
import org.scalamock.scalatest.MockFactory
import org.scalatest.time.{Millis, Seconds, Span}
import org.scalatest.{WordSpec, Matchers}

class MarkServiceImplTest extends WordSpec with Matchers with MockFactory {
//  implicit val defaultPatience =
//    PatienceConfig(timeout = Span(5, Seconds), interval = Span(500, Millis))

  "Mark service" should {
    "send mark" in {
//      val transport = stub[Transport]
//      when(transport.send
//      val markService = new MarkServiceImpl()
//      true should === (true)
    }
  }

}
